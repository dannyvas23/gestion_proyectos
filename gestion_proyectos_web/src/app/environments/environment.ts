// Configuración de desarrollo
// Para defender: este archivo se usa durante `ng serve`.
// Angular CLI reemplaza este archivo por environment.prod.ts al compilar con --configuration=production.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:9000/api',
  hubUrl: 'http://localhost:9000/hub/tablero'
};
