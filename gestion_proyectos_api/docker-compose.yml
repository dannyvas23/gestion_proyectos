services:
  gestion_proyectos_api:
    image: ${DOCKER_REGISTRY-}gestionproyectosapi
    build:
      context: .
      dockerfile: gestion_proyectos_api/Dockerfile

  postgresdb:
    image: postgres:17

volumes:
  postgres_postgresdb: